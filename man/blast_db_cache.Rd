% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/BiocFileCache.R
\name{blast_db_cache}
\alias{blast_db_cache}
\alias{blast_db_get}
\title{Cache BLAST Database Archives}
\usage{
blast_db_cache()

blast_db_get(
  file = "16S_ribosomal_RNA.tar.gz",
  baseURL = "https://ftp.ncbi.nlm.nih.gov/blast/db/",
  check_update = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{file}{Character scalar giving the archive filename, for example
\code{"16S_ribosomal_RNA.tar.gz"}.}

\item{baseURL}{Character scalar giving the directory URL from which \code{file}
is downloaded. It must end in \code{/}.}

\item{check_update}{Logical scalar; check whether the remote file is newer
than the cached copy and download it when necessary.}

\item{verbose}{Logical scalar; report whether a file is downloaded or a
cached copy is returned.}
}
\value{
\itemize{
\item \code{blast_db_cache()} returns a \link[BiocFileCache:BiocFileCache]{BiocFileCache::BiocFileCache} object.
\item \code{blast_db_get()} returns a character vector containing the local path to
the cached archive.
}
}
\description{
Download BLAST database archives and manage local copies with
\link[BiocFileCache:BiocFileCache]{BiocFileCache::BiocFileCache}.
}
\details{
\code{blast_db_cache()} opens rBLAST's user-level cache. Its location is selected
by \code{\link[tools:R_user_dir]{tools::R_user_dir()}} and therefore depends on the operating system and
environment.

\code{blast_db_get()} returns a cached archive when available and downloads it
otherwise. With \code{check_update = TRUE}, the remote resource is checked and an
outdated cached copy is refreshed. Downloaded archives still need to be
extracted before their database prefix can be passed to \code{\link[=blast]{blast()}}. Large
NCBI databases can consist of multiple numbered archives; download and
extract every part into the same directory.
}
\examples{
\donttest{
## Download or retrieve an existing cached copy
##   (downloading ~70MB will take a little).
blast_db_get("16S_ribosomal_RNA.tar.gz")

## Extract the chached archive, then open the database using its prefix.
db_dir <- tempfile("16S-rRNA-")
dir.create(db_dir)
untar(blast_db_get("16S_ribosomal_RNA.tar.gz"), exdir = db_dir)
db <- blast(file.path(db_dir, "16S_ribosomal_RNA"))
db
}

## Inspect the underlying cache with BiocFileCache functions.
cache <- blast_db_cache()
BiocFileCache::bfcinfo(cache)
}
\seealso{
Other blast:
\code{\link[=blast]{blast()}},
\code{\link[=makeblastdb]{makeblastdb()}}
}
\author{
Michael Hahsler
}
\concept{blast}
